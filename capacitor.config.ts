import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'br.com.eusouopeu.cifrasgroup',
  appName: 'cifrasGroup',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
}

export default config
